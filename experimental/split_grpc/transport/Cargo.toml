[package]
name = "split_grpc_transport"
version = "0.1.0"
authors = ["Conrad Grobler <grobler@google.com>"]
edition = "2018"

[dependencies]
async-trait = "*"
structopt = "*"
tokio = { version = "*", features = ["macros", "stream", "tcp", "uds"] }
tonic = "*"
